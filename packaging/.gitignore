# Build-time working directories and outputs created by Build-MsixPackage.ps1.
# obj/ holds the intermediate `dotnet publish` output and the staged package layout;
# dist/ holds the final .msix (and, when -Sign is used, the exported .cer). Both are
# regenerated by re-running the script and should not be committed.
obj/
dist/

<Window x:Class="SinhalaInput.App.CandidateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EasyAkuru"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        Visibility="Collapsed">
    <Border Background="#FF2B2B2B"
            BorderBrush="#FF565656"
            BorderThickness="1"
            CornerRadius="3"
            Padding="6">
        <StackPanel MinWidth="120">
            <TextBlock x:Name="PrimaryText"
                       Text="මම"
                       FontSize="18"
                       Foreground="White"
                       Margin="2,0,2,4" />
            <ItemsControl x:Name="AlternatesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   Foreground="#FFD0D0D0"
                                   FontSize="14"
                                   Margin="2,1" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
